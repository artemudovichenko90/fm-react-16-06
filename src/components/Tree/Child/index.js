import React from 'react';
import UserCard from '../UserCard';

const Child = (props) => {
  return (
    <section>
      <h2>Child</h2>
      <UserCard />
    </section>
  );
}

export default Child;
